// TODO: when the backend exposes a `/permissions` CRUD resource, define the
// RTK Query slice here. Today the role-capability matrix is purely
// client-side (see ../constants/permissionsConstants.ts).
export {};
