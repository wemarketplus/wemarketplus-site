// Reserved for zod schemas (create/edit permission form) once the backend
// exposes a permission resource. Empty today because the backend is purely
// role-based — see this module's index.ts header.
export {};
