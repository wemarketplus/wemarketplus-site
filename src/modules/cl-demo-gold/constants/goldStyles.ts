// Gold-specific static Tailwind class-string tokens (the generic, cross-module
// tokens live in `@/shared/cl-demo` styles). Static values only.

// Make-Ready task chip base class (reference .badge button in rMakeReady).
export const MR_CHIP_BASE = 'inline-block cursor-pointer rounded-full border-none px-2 py-0.5 text-[10px] font-bold';
